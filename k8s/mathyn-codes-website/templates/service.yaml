apiVersion: v1
kind: Service
metadata:
  name: {{ include "mathyn-codes-website.fullname" . }}
  labels:
    {{- include "mathyn-codes-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mathyn-codes-website.selectorLabels" . | nindent 4 }}
